# Private Credit

Private Credit is designed to show a private credit result backed by an encrypted on-chain profile.

The current frontend deliberately avoids turning ordinary wallet balances or transaction counts into a fabricated credit score. A live result requires the corresponding encrypted contract/profile to be deployed and configured.
